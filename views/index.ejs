<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">

    <title>Nodejs</title>
</head>
<body class="container">
    <%- include('partials/header.ejs') %>
    <%- include('partials/nav.ejs') %>


        <center>
        <img src="/img/upsinlogo.png" alt="" width="200px">   
        </center>

        <center>
            <h3><%=titulo%></h3>
        </center>

        <section>
            <center>
                <hr>
                <table class="table">
                    <tr>
                        <th>Matricula</th>
                        <th>Nombre</th>
                        <th>Sexo</th>
                        <th>Materias</th>
                    </tr>
                    <%
                        for(i=0;i<listado.length;i++){ %>

                            <tr>
                                <td><%=listado[i].matricula%></td>
                                <td><%=listado[i].nombre%></td>
                                <td><%=listado[i].sexo%></td>
                                <td><%=listado[i].materias%></td>
                            </tr>
                    <%
                        }
                        %>
                </table>
            </center>
        </section>
    </header>
    <%- include('partials/footer.ejs') %>
</body>
<script>
    function validar(){
        var pinicial = document.getElementById('pinicial').value;
        if (pinicial < 0) { alert("El porcentaje debe de ser mayor a 0"); return false;}
        if (pinicial > 100) { alert("El porcentaje debe de ser menor a 100"); return false;}
        return true;
    }        
</script>
</html>